{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "name": "${name}",
  "description": "${description}",
  "authors": ["${author}"],
  "contact": {
    "homepage": "${websiteUrl}",
    "issues": "${issueTrackerUrl}"
  },
  "license": "${license}",
  "icon": "assets/oldnewcombat/icon.png",
  "environment": "*",
  "entrypoints": {
    "main": ["straywave.minecraft.oldnewcombat.fabric.Entrypoint"]
  },
  "mixins": [
    "oldnewcombat.mixins.json",
    "oldnewcombat.fabric.mixins.json"
  ],
  "depends": {
    "fabric": ">=${fabricVersion}",
    "minecraft": "${minecraftVersion}",
    "architectury": ">=${architecturyVersion}"
  },
  "custom": {
    "modmenu": {
      "links": {
        "modmenu.discord": "https://discord.gg/yX7u32cKuN"
      }
    }
  }
}
